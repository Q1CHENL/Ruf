- Keep rapid consecutive Command-Tab gestures from being dropped or applied to the wrong target while apps are still loading.
- Make fast multi-display window moves more reliable when Reduce Motion is enabled.
- Stop showing Reopen when a Space query fails instead of proving the window is on another Space.
- Retry New Window after a stale Accessibility menu action instead of failing immediately.
- Keep the diagnostic log size limit enforced for long-running debug sessions.
